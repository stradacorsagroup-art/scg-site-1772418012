name: Inventory Sync (Manual Only)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Manual mode notice
        run: |
          echo "Automatic Notion sync has been disabled."
          echo "Inventory updates are now managed manually."
